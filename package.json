{
  "name": "kenpos-react",
  "version": "1.0.0",
  "private": true,
  "description": "A responsive, offline-capable, web-based Point of Sale (POS) system.",
  "scripts": {
    "dev": "python3 -m http.server 3000",
    "build": "mkdir -p dist && cp -r . dist/ && rm -rf dist/node_modules dist/.git dist/dist",
    "preview": "cd dist && python3 -m http.server 3000",
    "start": "python3 -m http.server 3000",
    "lint": "echo \"ESLint not configured for static project.\""
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "engines": {
    "node": ">=18.20.4",
    "npm": ">=10.0.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "@vitejs/plugin-react": "^4.0.0",
    "typescript": "^5",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@types/qrcode": "^1.5.5",
    "@types/jsbarcode": "^3.11.4"
  }
}
